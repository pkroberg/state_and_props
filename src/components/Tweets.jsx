import React from "react";

const Tweets = (props) => (
  <section>
    <h1>{props.me}</h1>
    <h2>{props.age}</h2>
    <p>tweet tweet</p>
    <p>tweet tweet</p>
    <p>tweet tweet</p>
  </section>
);

export default Tweets;
